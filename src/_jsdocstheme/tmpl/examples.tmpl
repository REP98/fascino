<?js
	var data = obj;
	var self = this;

	data.forEach(function(example) {
		if (example.caption) {
	?>
		<p class="code-caption"><?js= example.caption ?></p>
	<?js } ?>
	<pre class="prettyprint source line-numbers">
<code class="<?js if (example.lang) { ?> language-<?js=example.lang ?> <?js } ?>"><?js= self.htmlsafe(example.code) ?></code></pre>
<?js
	});
?>